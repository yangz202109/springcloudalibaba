server:
  port: 3377

spring:
  application:
    name: config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848  #服务注册中心地址
      config:
        server-addr: localhost:8848  #配置中心地址
        file-extension: yaml  #指定yaml格式的配置

        #namespace: 7e30d153-cac4-4f4c-8ad9-1adf613b20f4  #指定在哪个命名空间
        #group: DEFAULT_GROUP #指定在哪个group里
